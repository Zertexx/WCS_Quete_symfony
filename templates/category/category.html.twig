{% extends 'templates/base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}


    <h1>Résultat = {{ category.name }} </h1>

    {% for article in articles %}
        <h2>{{ article.title }}</h2> <br>
        <p>{{ article.content }}</p> <br>


    {% endfor %}

{% endblock %}